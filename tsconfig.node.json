{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "composite": true,
    "types": ["node"]
  },
  "//": "src/meta.ts is the one source for the page metadata, read by the build and imported by the application, so it belongs to both projects.",
  "include": ["vite.config.ts", "src/meta.ts"]
}
